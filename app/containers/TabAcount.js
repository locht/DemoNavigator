import * as React from 'react';
import { Text, View } from 'react-native';

class TabAcount extends React.Component {
    render() {
        return (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center'
                }}>
                <Text>Acount!</Text>
            </View>
        );
    }
}

export default TabAcount;
